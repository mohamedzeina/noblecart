<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/admin.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

        <main>
            <div class="admin-page-header">
                <div class="admin-page-header__left">
                    <h1 class="admin-page-header__title">My Products</h1>
                    <p class="admin-page-header__subtitle"><%= prods.length %> product<%= prods.length !== 1 ? 's' : '' %> listed</p>
                </div>
                <a href="/admin/add-product" class="btn btn--solid">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" width="15" height="15">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                    </svg>
                    Add Product
                </a>
            </div>

            <% if (prods.length > 0) { %>
                <% const catLabels = { electronics: 'Electronics', fashion: 'Fashion', home: 'Home', accessories: 'Accessories' }; %>
                <div class="admin-product-list">
                    <% for (let product of prods) { %>
                        <article class="admin-product-item">
                            <div class="admin-product-item__thumb">
                                <img src="<%= product.imageUrl %>" alt="<%= product.title %>" loading="lazy">
                            </div>
                            <div class="admin-product-item__info">
                                <h2 class="admin-product-item__title"><%= product.title %></h2>
                                <div class="admin-product-item__meta">
                                    <span class="admin-product-item__price">$<%= product.price.toFixed(2) %></span>
                                    <% if (product.category) { %>
                                        <span class="admin-product-item__category"><%= catLabels[product.category] || product.category %></span>
                                    <% } %>
                                    <% if (product.modelUrl) { %>
                                        <span class="admin-product-item__badge--3d">3D</span>
                                    <% } %>
                                </div>
                                <p class="admin-product-item__desc"><%= product.description %></p>
                            </div>
                            <div class="card__actions admin-product-item__actions">
                                <a href="/admin/edit-product/<%= product._id %>?edit=true" class="btn btn--solid">Edit</a>
                                <input type="hidden" value="<%= product._id %>" name="productId">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                                <button class="btn delete-btn" type="button">Delete</button>
                            </div>
                        </article>
                    <% } %>
                </div>
                <%- include('../includes/pagination.ejs', {currentPage: currentPage, nextPage: nextPage, prevPage: prevPage, lastPage: lastPage, hasNextPage: hasNextPage, hasPrevPage: hasPrevPage}) %>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-state__icon">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="48" height="48">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v6m3-3H9m12 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                        </svg>
                    </div>
                    <h2 class="empty-state__title">No products yet</h2>
                    <p class="empty-state__subtitle">Get started by adding your first product.</p>
                </div>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>
        <script src="/js/admin.js"></script>
